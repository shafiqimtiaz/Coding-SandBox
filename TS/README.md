# TypeScript Practice

Interview problems as runnable single files, numbered by priority (`00` essentials first, `01`–`10` must-know, `11`–`17` very useful, unnumbered extras).

Setup once:

```sh
npm install
```

Run any file, typecheck everything:

```sh
npm run code -- 01-two-sum.ts
npm run compile
```
